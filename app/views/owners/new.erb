<h1>Create a new Owner</h1>

<form action="/owners" method="POST">
  <label>Name:</label>

  <br>

  <% #Wherever we see the owner[:key_name] owner is referring to top level :owner key in hash... %>
  <% #anything inside it like owner[name] will make {"owner"=>{"name" => text_input}} %>
  <input type="text" name="owner[name]">

  <h3>Existing Pets</h3>
  <% @pets.each do |pet| %>
    <input type="checkbox" 
      name="owner[pet_ids][]" 
      id="<%= pet.id %>"  <% #id is optional i think %>
      value="<%= pet.id %>" <% #we need a value= so we can assign what a checkbox holds as a value (to put in array) %>
      
      <% #Why do we give it a value= here but not for creating a new pet? %>
      <% #How does the "owner" => {"pet_ids"=>["1", "2"]} get the string id here? %>
    <%= pet.name %>
    > <% #end of input form %>
    <br>
  <% end %>
  <br>

  <h3>New Pet</h3>
  <label>Pet Name: </label>
  <br>
  <input type="text" name="pet[name]" id="pet_name"></input>
  <br><br>

  <input type="submit" value="Create Owner">
</form>
